{
  "name": "school-management-system",
  "version": "1.0.0",
  "description": "School Management System root configuration",
  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "cd backend && .venv/bin/uvicorn app.main:app --reload"
  },
  "dependencies": {
    "lucide-react": "^1.30.0"
  }
}
